<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone"
                tools:showIn="@layout/ucrop_activity_photobox"
                tools:visibility="visible">

    <TextView
        android:id="@+id/textViewRotateAngle"
        style="@style/ucrop_TextViewWidgetText"
        android:text="100°"/>

    <com.yalantis.ucrop.view.widget.HorizontalProgressWheelView
        android:id="@+id/rotateScrollWheel"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/textViewRotateAngle"
        android:layout_toEndOf="@+id/wrapperResetRotate"
        android:layout_toLeftOf="@+id/wrapperRotateByAngle"
        android:layout_toRightOf="@+id/wrapperResetRotate"
        android:layout_toStartOf="@+id/wrapperRotateByAngle"/>

    <FrameLayout
        android:id="@+id/wrapperResetRotate"
        style="@style/ucrop_WrapperRotateButton"
        android:layout_centerVertical="true">

        <ImageView
            style="@style/ucrop_ImageViewWidgetIcon"
            android:src="@drawable/ucrop_ic_reset"/>

    </FrameLayout>

    <FrameLayout
        android:id="@+id/wrapperRotateByAngle"
        style="@style/ucrop_WrapperRotateButton"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true">

        <ImageView
            style="@style/ucrop_ImageViewWidgetIcon"
            android:src="@drawable/ucrop_ic_angle"/>

    </FrameLayout>

</RelativeLayout>